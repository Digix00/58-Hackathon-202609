# backend(Cloudflare Workers)のURL。未設定時はwrangler devの既定ポートを見る。
VITE_API_BASE_URL=http://localhost:8787

# LINE Developers Console で発行した LIFF ID。本番環境または実際のLINE認証の確認時に設定する。
VITE_LINE_LIFF_ID=

# ローカル開発ではLIFF SDKを使わないデバッグモードを既定にする。
# 実際のLINE認証を確認する場合はfalseに変更する。
VITE_DEV_LIFF_MODE=true

# ローカルAPIの開発用認証を使う場合はbackendにする。
# 開発時のみ有効。本番ビルドでは無効になる。
VITE_DEV_AUTH_MODE=backend

# VITE_DEV_AUTH_MODE=backend のときに使用する固定の開発用ユーザー。
VITE_DEV_USER=demo-a
